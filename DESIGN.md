# DESIGN.md

> 记录于 2026-07 全站外观改造完成后，从建成代码归纳（ground truth over intention）。
> v4 (2026-07) 更新：用户反馈橙色刺激，整体改为"主要黑与白"。

## 视觉世界：黑白极简（Monochrome）

TaskPPS Web 的视觉语言。早期「工程蓝图」冷 slate 体系 → v3「暖橙工作台」n8n 橙 #FF6D5A
→ v4 黑白化。用户锁定的方向：主要黑与白，仅保留语义状态色。

### 设计机制（一句话）
一个自托管编排工具台 —— 中性灰打底，黑色贯穿"动作/激活/运行"，
日志与 REPL 用深色终端面板制造对焦时刻。拒绝高饱和品牌色与"同尺寸卡片堆砌"。

### 色彩（token 来源：`web/src/theme/antd.ts`、`web/src/index.css`、`nodes/nodeTokens.ts`）
- 背景中性灰 `#F5F5F5`，内容区近白 `#FAFAFA`，表面纯白 `#FFFFFF`
- 主色黑 `#1F1F1F`（hover `#3A3A3A`，软调 `#F0F0F0`，选中底 `rgba(31,31,31,0.10)`）
- 墨色 `#262626` / `#525252` / `#8C8C8C`（近黑三级）
- 发丝线 `#E0E0E0`（表格/输入框边框），分割线 `#E8E8E8`
- 语义状态色：成功 `#10B981`、失败 `#EF4444`、跳过 `#F59E0B`、运行 `#1F1F1F`、
  pending/cancelled `#8C8C8C`；类型色（节点/命令）沿用原语义色
- 阴影：柔和多层 `rgba(30,25,20,0.06/0.04)`；**elevation 二选一** —— 卡片去边框用阴影，
  表格/输入框用发丝边框，避免幽灵卡片

### 字体与排版
- Inter + Noto Sans（UI 文本）、JetBrains Mono（任务名/类型/日志/代码）
- 圆角：控制件 8、卡片 12、登录卡 16；数据表格紧凑行高

### 组件语言
- 侧栏/顶栏白底 + 发丝边框；菜单激活态黑灰软底
- 主按钮黑色实心（白字）；链接/激活态黑色
- 卡片柔和阴影去边框；表格表头近白、行 hover 黑灰调
- 状态徽章（StatusTag）软底 + 色点；DAG 节点白卡 + 类型色图标 + 状态晕染边框，
  运行中节点黑色脉冲，选中黑色描边

### 签名元素
- **DAG 画布**：暖白底 + 点阵（#CBD5E1 dots，与查看模式一致）
- **深色终端面板**（LogViewer / REPL / YAML 编辑器右侧）：`#111827` 底 +
  终端配色日志行 —— 浅色 UI 中的"闪亮"对焦点（One-KVM 启发）
- **登录页**：近白底 + 左上角极淡黑灰径向光晕 + 居中白卡

### 约束与边界
- 页面结构/路由/文案/功能逻辑未改动；不改结构以保证 e2e 稳定
- `ReplModal.tsx`（root 属主、无写权限）保留少量冷灰 `#3D5BFF/#7C7F88/#E3E4E8`，
  属已知遗留，建议部署前 chown 后统一
- 保持 #94A3B8（DAG 边）与 #CBD5E1（画布点阵）不暖化，因有测试断言
